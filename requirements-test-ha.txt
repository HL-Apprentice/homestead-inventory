# Heavy dependencies for the end-to-end HTTP harness (tests_ha/).
# Pinned together for reproducibility: PHCC bundles a specific Home Assistant,
# which in turn pins the frontend wheel version below. Requires Python 3.13.
pytest-homeassistant-custom-component==0.13.316
home-assistant-frontend==20260128.6
aiosqlite>=0.20.0
