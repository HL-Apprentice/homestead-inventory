{
  "config": {
    "step": {
      "user": {
        "title": "Homestead Inventory",
        "description": "Rooms > Cupboards > Shelves > Organizers > Items",
        "data": {
          "allow_structure_modification": "Allow structure modification",
          "require_admin": "Restrict access to admin users only (panel and API)",
          "enable_barcode_lookup": "Enable barcode product lookup (sends scanned barcodes to Open Food Facts)"
        }
      }
    },
    "abort": {
      "already_configured": "Homestead Inventory is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Homestead Inventory options",
        "data": {
          "allow_structure_modification": "Allow structure modification",
          "require_admin": "Restrict access to admin users only (panel and API)",
          "enable_barcode_lookup": "Enable barcode product lookup (sends scanned barcodes to Open Food Facts)"
        }
      }
    }
  }
}
